{
  "mcpServers": {
    "mcp-sql-server-production": {
      "command": "node",
      "args": [
        "-e",
        "const http = require('http'); const req = http.request({hostname: '192.168.40.197', port: 3000, path: '/mcp', method: 'POST'}, (res) => { res.pipe(process.stdout); }); process.stdin.pipe(req);"
      ]
    }
  }
}
